# digit-recognition-svm
# Digit Recognition using SVM

This project uses Support Vector Machine to recognize handwritten digits.

## Steps
- Load dataset
- Train model
- Predict digits

## Run
pip install -r requirements.txt
python digit_svm.py
